{
  "private": true,
  "name": "kip-functions",
  "version": "0.0.0",
  "description": "Cloud Functions for kip: serving public share links, and (later) notification email.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@types/nodemailer": "^8.0.1",
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "typescript": "~7.0.2"
  }
}
